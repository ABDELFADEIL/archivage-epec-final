spring:
  jackson:
    serialization:
      fail-on-empty-beans: false
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/archivage?serverTimezone=UTC
    username: root
    password: adminbeel
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: '@project.name@'
  jpa:
    mapping-resources:
      - META-INF/account.orm.xml
      - META-INF/classificationNature.orm.xml
      - META-INF/client.orm.xml
      - META-INF/contract.orm.xml
      - META-INF/document.orm.xml
      - META-INF/event.orm.xml
      - META-INF/history.orm.xml
      - META-INF/user.orm.xml
      - META-INF/destructionList.orm.xml
    open-in-view: false
    database: mysql
    cache:
      jcache:
        config: classpath:cache/ehcache.xml
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format-sql: false
        use-sql-coments: false
        generate-statistics: false
    show-sql: true


management:
  endpoints:
    web:
      exposure:
        include: info, health, configprops, logfile, metrics, env, loggers, httptrace, mappings, threaddump
    application:
  version: '@project.version@'
  timezone: ${user.timezone}

server:
  port: 8080
  ssl:
    enabled: false
